spring:
  profiles:
    active: native
  cloud:
    config:
      server:
        native:
          search-locations: file:/S:/TOOLS/.Hudyma_projects/BlaBlaCar2025/config
management:
  endpoint:
    health:
      show-details: always